import React from 'react';

import styles from './style.scss';

const MainPage = (): JSX.Element => (
  <React.Fragment>
    <div className={styles['main-page-wrapper']}>Main Page</div>
  </React.Fragment>
);

export default MainPage;
